# Exchange API Configuration (if using authenticated endpoints)
# Most exchanges require API keys for trading
# For read-only market data, API keys are usually not required

EXCHANGE_API_KEY=your_api_key_here
EXCHANGE_API_SECRET=your_api_secret_here

# Optional: Testnet/Sandbox Mode
USE_TESTNET=true
